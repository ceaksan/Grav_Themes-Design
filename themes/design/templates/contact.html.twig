{% extends 'partials/base.html.twig' %}

  {% block head_css %}
		{{ parent() }}
  {% endblock %}

  {% block body %}
    {% block body_content %}

      <div class="py-5" data-aos="fade-up" data-aos-delay="0">
        <div class="container">
          <div class="row align-items-stretch">
            
            <div class="col-lg-6 order-lg-2 mb-5 mb-lg-0">
              <div class="section-title mb-5">
                <strong class="subtitle" data-aos="fade-up" data-aos-delay="0">{{ page.title }}</strong>
                <h2 class="heading" data-aos="fade-up" data-aos-delay="100">{{ page.header.heading }}</h2>
              </div>
              {% include "forms/form.html.twig" %}
            </div>

            <div class="col-lg-5 mr-auto">
              {% include "partials/testimonial.html.twig" %}
            </div>

          </div>
        </div>
      </div>
    {% endblock %}
  {% endblock %}